package com.vastika.rating.data.service.DTO;
/*
import org.springframework.beans.factory.annotation.Autowired;

import com.vastika.rating.data.service.model.Authentication;
import com.vastika.rating.data.service.model.Role;
import com.vastika.rating.data.service.model.UserInfo;

import lombok.Data;

@Data

public class UserLoginValidation {
	@Autowired
	private Authentication authentication;
	@Autowired
	private Role role;
	
	private int userId;
	private String username = authentication.getUsername();
	private String password = authentication.getPassword();
	private boolean ActiveStatus = true ;
	private String roles = role.getRoleName();
	
	
	public UserLoginValidation(int userId, String username, String password, boolean activeStatus, String roles) {
		super();
		this.userId=userId;
		this.username = username;
		this.password = password;
		ActiveStatus = activeStatus;
		this.roles = roles;
	}
	

}
*/
